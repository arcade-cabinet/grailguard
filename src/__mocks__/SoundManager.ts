export const soundManager = {
  init: jest.fn(),
  playAmbience: jest.fn(),
  stopAmbience: jest.fn(),
  playMusic: jest.fn(),
  stopMusic: jest.fn(),
  playUiClick: jest.fn(),
  playBuild: jest.fn(),
  playCombat: jest.fn(),
  playGameOver: jest.fn(),
};
